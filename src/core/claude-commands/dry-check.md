# DRY Check

Run a focused reuse analysis against the codebase — equivalent to Phase 3 (DRY Analysis) from `/lead`.

Read the Phase 3 section of `.agent-runway/skills/lead/SKILL.md` and execute the codebase searches it defines. Do not run any other phase.

Report every reuse opportunity found: existing validators, similar commands/queries, shared base classes, extension methods, utilities, EF Core query patterns, and configuration or constants already defined.
