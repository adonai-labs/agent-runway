# Ticket Creator — Project Configuration

<!-- Populate with project-specific settings to enable richer ticket output.
     When this file is empty or absent, the skill runs in generic mode.

     Sections to configure:
     - project: jira_project_key, jira_board_url
     - personas: list of real user roles (e.g. geologist, project manager)
     - docs: paths to business/contract/architecture docs
     - codebase_scanning: enabled flag, root paths, stack
     - output: default_destination (jira | markdown | both), markdown_path
     - agent_notes_gate: require_named_module, require_constraint
     - completeness: auto_fix_max_iterations, mark_inferred_content
     - complexity: thresholds for split suggestions
-->
