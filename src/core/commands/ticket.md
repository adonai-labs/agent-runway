# Ticket

Read and follow the skill at `.cursor/skills/ticket-creator/SKILL.md`.

Create a development-ready ticket from chat context, a markdown requirement, or an approved spec (optional).

Keep the ticket output concise, scoped, and executable by `lead`. When saving markdown, use **Artefact naming** from the skill (`task-<nn>-<implementation-slug>-<slice>.md`, or epic/spec names under the same implementation folder).

