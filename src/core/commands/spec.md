# Spec

Read and follow the skill at `.cursor/skills/spec-creator/SKILL.md`.

Create or refine a spec-first change definition with intent, requirements, scenarios, design notes, execution plan, and quality gates.

Output should be saved under `.agent-runway/specs/proposed/<implementation-slug>/` using filenames that include the implementation slug (for example `<implementation-slug>-spec.md`), never a bare `spec.md`.

